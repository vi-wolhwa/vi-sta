/**
 * @vi-sta/presets — opt-in keyword, embed, and theme bundles.
 *
 * Subpath entries (/keywords, /embeds, /themes) are added from M6–M7.
 * This is the M0 scaffold entry point.
 */
export const version = '0.0.0';
