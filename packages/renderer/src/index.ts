/**
 * @vi-sta/renderer — pure, DOM-free keyword-to-HTML renderer.
 *
 * Implemented from M8. Kept independent of @vi-sta/core so that
 * published content can be rendered without pulling in the editor engine.
 */
export const version = '0.0.0';
